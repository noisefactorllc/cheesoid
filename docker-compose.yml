services:
  cheesoid:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PERSONA=${PERSONA:-example}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - PORT=3000
    volumes:
      - ./personas:/app/personas
      - ./log:/app/log
    restart: always
